# 🔧 CORREÇÕES PONTUAIS: DespesaForm.jsx

## 📍 Arquivo: `src/components/DespesaForm.jsx`

---

## ✅ CORREÇÃO 1: Linha 313 (Carregamento do valor para edição)

### 🔍 Localização
Dentro do `useEffect` que carrega `despesaParaEditar` (por volta da linha 304-360)

### ❌ ANTES (BUGADO):
```javascript
const dadosCarregados = {
  emendaId: despesaParaEditar.emendaId || "",
  discriminacao: despesaParaEditar.discriminacao || "",
  fornecedor: despesaParaEditar.fornecedor || "",
  valor: despesaParaEditar.valor?.toString() || "",  // ❌ BUG: valor 0 vira ""
  numeroEmpenho: despesaParaEditar.numeroEmpenho || "",
  // ... resto dos campos
};
```

### ✅ DEPOIS (CORRIGIDO):
```javascript
const dadosCarregados = {
  emendaId: despesaParaEditar.emendaId || "",
  discriminacao: despesaParaEditar.discriminacao || "",
  fornecedor: despesaParaEditar.fornecedor || "",
  valor: despesaParaEditar.valor != null ? String(despesaParaEditar.valor) : "",  // ✅ CORRIGIDO
  numeroEmpenho: despesaParaEditar.numeroEmpenho || "",
  // ... resto dos campos
};
```

### 📝 Explicação:
- `despesaParaEditar.valor?.toString() || ""` → Quando valor é `0`, vira `""` (string vazia)
- `despesaParaEditar.valor != null ? String(valor) : ""` → Preserva `0` corretamente

---

## ✅ CORREÇÃO 2: Linha 416 (Salvamento do valor no Firebase)

### 🔍 Localização
Dentro da função `handleSubmit`, no objeto `dadosParaSalvar` (por volta da linha 412-437)

### ❌ ANTES (BUGADO):
```javascript
const dadosParaSalvar = {
  ...formDataLimpo,
  emendaId:
    formData.emendaId || despesaParaEditar?.emendaId || emendaId || "",
  valor: parseFloat(formData.valor) || 0,  // ❌ BUG: "R$ 250,00" vira NaN → 0
  contrapartida: parseFloat(formData.contrapartida) || 0,
  percentualExecucao: parseFloat(formData.percentualExecucao) || 0,
  // ... resto dos campos
};
```

### ✅ DEPOIS (CORRIGIDO):
```javascript
const dadosParaSalvar = {
  ...formDataLimpo,
  emendaId:
    formData.emendaId || despesaParaEditar?.emendaId || emendaId || "",
  valor: parseValorMonetario(formData.valor),  // ✅ CORRIGIDO
  contrapartida: parseFloat(formData.contrapartida) || 0,
  percentualExecucao: parseFloat(formData.percentualExecucao) || 0,
  // ... resto dos campos
};
```

### 📝 Explicação:
- `parseFloat("R$ 250,00")` → `NaN` → `0` ❌
- `parseValorMonetario("R$ 250,00")` → `250` ✅
- A função já está importada na linha 64!

---

## 🎯 RESULTADO ESPERADO

Após aplicar as 2 correções:

### ✅ AO EDITAR DESPESA COM VALOR R$ 0,00:
- Campo carrega com `"0"` (não some mais)
- Pode alterar para qualquer valor
- Salva corretamente no Firebase

### ✅ AO CRIAR/EDITAR DESPESA COM VALOR R$ 250,00:
- Campo aceita formatação brasileira `"R$ 250,00"`
- Converte corretamente para `250` ao salvar
- Aparece na tabela como **R$ 250,00** (não mais R$ 0,00)

---

## 📋 CHECKLIST DE APLICAÇÃO

- [ ] Abrir `src/components/DespesaForm.jsx`
- [ ] Localizar linha 313 (dentro do useEffect de despesaParaEditar)
- [ ] Substituir `valor: despesaParaEditar.valor?.toString() || ""`
- [ ] Por `valor: despesaParaEditar.valor != null ? String(despesaParaEditar.valor) : ""`
- [ ] Localizar linha 416 (dentro de handleSubmit > dadosParaSalvar)
- [ ] Substituir `valor: parseFloat(formData.valor) || 0`
- [ ] Por `valor: parseValorMonetario(formData.valor)`
- [ ] Salvar arquivo
- [ ] Testar criando despesa com R$ 250,00
- [ ] Testar editando despesa com valor R$ 0,00
- [ ] Verificar que valores aparecem corretamente na tabela

---

## 🐛 SE CONTINUAR COM PROBLEMA

Caso ainda persista, verifique:

1. ✅ A função `parseValorMonetario` está importada? (linha 64)
2. ✅ O arquivo `formatters.js` tem essa função implementada?
3. ✅ Não há outro `parseFloat(formData.valor)` perdido no código?

---

## 🎉 SUCESSO!

Com essas 2 correções, o sistema estará:
- ✅ Carregando valores corretamente para edição (inclusive zero)
- ✅ Convertendo valores formatados (R$) para número ao salvar
- ✅ Exibindo valores corretos na tabela de despesas

**Sistema 100% funcional para valores monetários!** 🚀