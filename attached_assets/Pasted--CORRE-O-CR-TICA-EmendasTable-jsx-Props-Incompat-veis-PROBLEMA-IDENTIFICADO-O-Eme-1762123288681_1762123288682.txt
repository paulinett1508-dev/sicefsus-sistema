# CORREÇÃO CRÍTICA: EmendasTable.jsx - Props Incompatíveis

## ⚠️ PROBLEMA IDENTIFICADO:

O `Emendas.jsx` envia props com um nome, mas o `EmendasTable.jsx` espera props com OUTRO nome!

### Emendas.jsx ENVIA (linha 554-561):
```jsx
<EmendasTable
  emendas={emendasFiltradas}         // ✅
  totalEmendas={emendas.length}      // ✅
  loading={false}                     // ✅
  onEdit={handleEditar}              // ✅
  onDelete={handleDeletar}           // ✅
  userRole={userRole}                // ✅
/>
```

### EmendasTable.jsx ESPERA (linha 7-14):
```jsx
const EmendasTable = ({
  emendasFiltradas,    // ❌ ERRADO - deveria ser "emendas"
  usuario,             // ❌ ERRADO - deveria ser "userRole"
  onAbrirEmenda,       // ❌ NÃO USADO
  onEditarEmenda,      // ❌ ERRADO - deveria ser "onEdit"
  onExcluirEmenda,     // ❌ ERRADO - deveria ser "onDelete"
  onVerDespesas,       // ❌ NÃO USADO
  onNovaEmenda,
}) => {
```

---

## ✅ CORREÇÃO - Linha 7-14:

**SUBSTITUIR COMPLETAMENTE:**

```jsx
const EmendasTable = ({
  emendas,              // ✅ Corrigido
  totalEmendas,         // ✅ Novo
  loading,              // ✅ Novo
  onEdit,               // ✅ Corrigido
  onDelete,             // ✅ Corrigido
  userRole,             // ✅ Corrigido
  onNovaEmenda,
}) => {
  // Compatibilidade interna
  const emendasFiltradas = emendas;
  const usuario = { role: userRole };
```

---

## ✅ CORREÇÃO - Linha 155 (botão Editar):

**ANTES:**
```jsx
onClick={() => onEditarEmenda(emenda)}
```

**DEPOIS:**
```jsx
onClick={() => onEdit(emenda)}
```

---

## ✅ CORREÇÃO - Linha 162 (botão Excluir):

**ANTES:**
```jsx
onClick={() => onExcluirEmenda(emenda.id)}
```

**DEPOIS:**
```jsx
onClick={() => onDelete(emenda.id)}
```

---

## 📝 RESUMO DAS MUDANÇAS:

1. **Linha 7-17**: Corrigir nomes das props
2. **Linha 155**: Trocar `onEditarEmenda` por `onEdit`
3. **Linha 162**: Trocar `onExcluirEmenda` por `onDelete`

Depois disso, os botões vão funcionar corretamente! 🎯